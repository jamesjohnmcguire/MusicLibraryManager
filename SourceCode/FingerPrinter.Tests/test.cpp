#include "pch.h"

TEST(TestCaseName, TestName)
{
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}

TEST(TestFingerPrinter, Success)
{
	EXPECT_TRUE(true);
}

TEST(TestFingerPrinter, Correct)
{
	EXPECT_EQ(1, 1);
}
